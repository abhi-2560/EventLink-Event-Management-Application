# The migration engine that generates and applies schema changes to your database.
alembic==1.18.5

# Hashes passwords securely. Never store plain-text passwords.
bcrypt==5.0.0

# Implements signals/events inside Flask (for example, request started, request finished).
blinker==1.9.0

# Builds command-line interfaces
click==8.4.2

cloudinary==1.45.0
colorama==0.4.6
Flask==3.1.3
Flask-Cors==5.0.1

# Creates and verifies JWT access and refresh tokens
Flask-JWT-Extended==4.7.4

# Connects Flask with Alembic and provides commands like flask db migrate and flask db upgrade.
Flask-Migrate==4.1.0

# Gives you db.Model, db.session, and easy app integration.
Flask-SQLAlchemy==3.1.1

# used internally by SQLAlchemy
greenlet==3.5.4

# Generates and verifies signed tokens
itsdangerous==2.2.0

# Flask's templating engine for rendering HTML templates.
Jinja2==3.1.6

# Template engine used internally by Alembic when generating migration files.
Mako==1.3.12


# Used by Jinja2
MarkupSafe==3.0.3

# The PostgreSQL driver. SQLAlchemy uses this to actually communicate with a PostgreSQL database.
psycopg2-binary==2.9.12
PyJWT==2.13.0
python-dotenv==1.2.2

# The ORM and SQL toolkit.
SQLAlchemy==2.0.51
typing_extensions==4.16.0

#request/response objects, routing and dev server
Werkzeug==3.1.8
